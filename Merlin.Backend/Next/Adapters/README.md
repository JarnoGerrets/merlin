# Merlin Next adapters

This folder is intentionally inert in the skeleton phase.

Future adapter plans may add ports around external integrations such as model providers, BrowserHost IPC, frontend output, or audio systems. This folder must not perform side effects until a later plan wires a guarded runtime path.
